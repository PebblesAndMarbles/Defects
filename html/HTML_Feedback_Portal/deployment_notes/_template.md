---
workspace: [[WORKSPACE_NAME]]
date: YYYY-MM-DD
report: [[REPORT_NAME_OR_PATH]]
title_short: [[short-kebab-title]]
template_change_suggested: false
---

## What was deployed

<!-- Which report(s) got the feedback widget, and where in the target
     workspace the copied HTML_Feedback_Portal files ended up. -->

## Customizations made

<!-- FIELDNAMES changes, DATA_FILE location (local vs UNC), PER_USER_CSV
     on/off, itemKeys source (hardcoded vs dynamically generated), any
     form-field changes in frontend/index.html, launcher Python path
     discovery changes, etc. -->

## New use cases / edge cases encountered

<!-- Anything the template didn't anticipate: different feedback semantics
     (ratings vs status), multiple reports sharing one backend, identity/auth
     needs, non-Flask hosting constraints, etc. -->

## Bugs / friction encountered

<!-- Use a BUG-XXX style entry if useful. Include root cause and whether a
     fix was applied locally (and if so, whether it belongs in the shared
     template instead of just this workspace's copy). -->

## Suggested changes to the canonical template

<!-- Concrete suggestions for Agent_Templates\HTML_Feedback_Portal\ itself.
     Leave blank / "none" if this deployment didn't surface anything worth
     changing upstream. Do NOT edit the canonical template files directly —
     record the suggestion here for later review. -->

## Notes for future agents

<!-- Anything else worth knowing before reusing this deployment as a
     reference, or before someone reviews this note to update the shared
     template. -->
