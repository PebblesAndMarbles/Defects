agents_history\sessions\2026-09-04_001_small-particle-raw-cache-flat-raw-images-checkpoint.md
agents_history\sessions\2026-09-04_003_tranche-builder-manifest-matching-checkpoint.md

The first of these documented sessions began with running v8 of a prompt generation and I generally liked the vlm output - then the session pivoted towards generating an on-disk raw image cache which is now used to 1) generate the particle vlm labeled reports and 2) generate the beep misclassification reports.

- modify the canonical -> prompt generation python infrastructure.  we still have a bit of iteration to do, and I am concerned that we are generating a new python file, each iteration.  the v8 updates to the prompt at beginning of this session can be regarded as being about 85%, maybe 90% of the way there towards production attribute labeling and description.  can you review, generation images\Alloy_Class\tools\probe_generic_description_v8.py.  Is there opportunity to create a 'fixed' probe_generic_description py file, that accepts new prompt revisions, as a config?
- need to enrich production csv's with this VLM data, probably on adhoc basis to start until prompt is finalized (again, we are close).  VLM data enrichment should also be accompanied by the beep classification work I have done -- I think I have dispositioned upwards of 1500 defects, of the 6k+, so far.  Would be of interest, on one hand, to track what a *real* particle metric has been, over time that does not include misclassified BEEPs, and on the other, tracking metrics of certain particle types, e.g, circles, jagged particles, defect count > 1, etc.
- reporting infrastructure to generate an html, say, of filtered VLM labels.  I.e., generate an html report that shows circles, 'defect count > 1', or 'clumped' particles that I can then provide feedback, through a portal for review and prompt iteration.  On one hand, its of interest, say, to group certain filtered attributes, so we can say 'look these show beep evidence more often than others, but this image doesn't alone show evidence -- yet, is guilty by association.'

I'm a bit concerned by layout of the Alloy_Class folder.  We have tested a lot of ideas, generated a lot of results, and it is somewhat sprawling, despite still being somewhat contained.  As we are really close to pushing a lot of these ideas to some kind of production, and an analytical stage, should we, say, extract out the working components that we have finalized, including tools, so that we can 'start anew' ?  I like this ide abut recognize there is also a lot of work to do, and a new folder would probably become just as populated, quickly.  So I could see it being reasonable to just push on until everything is in production.